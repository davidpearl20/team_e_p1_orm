---
- hosts: all
  become: yes

  tasks:
    - name: install pip3
      package:
        name: python3-pip
        state: latest
    - name: install setuptools
      ansible.builtin.command:
        cmd: 'pip3 install --upgrade pip setuptools'
    - name: navigate to python folder
      ansible.builtin.command:
        cmd: 'cd /usr/bin/python'
        cmd: 'pip3 install psycopg2'
    - name: Create a new database
      community.postgresql.postgresql_db:
        name: airline_db
        